pluginManagement {
    repositories {
        gradlePluginPortal()
        // restdocs-api-spec 플러그인의 generator 아티팩트는 mavenCentral에 게시됨
        mavenCentral()
    }
}

rootProject.name = "flori-ai-api"
