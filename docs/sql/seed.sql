-- =============================================
-- 공유 시드 데이터 (테넌트 무관 참조 데이터)
-- all-tables-ddl.sql 적용 후 실행한다. 멱등(ON CONFLICT DO NOTHING).
-- =============================================
--
-- 현재 시드할 공유 참조 데이터가 없다.
-- (경매 시세는 단일 시장(aT 양재)이라 시장/법인 매핑 마스터가 필요 없다 — flower_markets 폐기.
--  시세 데이터 자체는 FlowerAuctionIngestService 의 @Scheduled cron 이 f001 API 에서 적재한다.)
