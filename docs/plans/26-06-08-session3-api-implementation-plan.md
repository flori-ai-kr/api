# 세션3 API 구현 계획 (사진↔고객 매핑 + 커스텀 등급)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development (권장) 또는 superpowers:executing-plans 로 task 단위 실행. 체크박스(`- [ ]`)로 추적.

**Goal:** Kotlin BFF에 (1) `photo_cards.customer_id` soft 참조와 (2) 테넌트별 커스텀 등급 + 구매횟수 자동승급/수동고정을 추가한다.

**Architecture:** FK 없는 soft 참조 + 앱레이어 테넌트 격리(`TenantContext.currentUserId()`) 기존 원칙 답습. 구매통계는 sales 실시간 집계(SSOT). 등급은 신규 `customer_grades` 테이블로 정의하고 `customers.grade_id`가 참조. 자동승급은 SaleService 변경 훅에서 재계산.

**Tech Stack:** Kotlin, Spring Boot, JPA(`ddl-auto: validate`), PostgreSQL(수동 마이그레이션 + `all-tables-ddl.sql` SSOT), JUnit5/REST Docs.

**기준 spec:** `../flori-web-session3/docs/plans/26-06-08-session3-customer-gallery-redesign.md`

**⚠️ DB 적용 규칙:** 마이그레이션 SQL은 작성만 하고 **RDS 적용은 사용자 사전 허락 후** 실행. `ddl-auto: validate` 이므로 앱 부팅 전 스키마가 엔티티와 일치해야 함 → 로컬/테스트는 testcontainers 또는 적용된 DB 기준.

---

## 파일 구조

**신규 생성**
- `docs/sql/migration/26-06-08-photo-cards-add-customer-id.sql`
- `docs/sql/migration/26-06-08-customer-custom-grades.sql`
- `src/main/kotlin/kr/ai/flori/customers/entity/CustomerGrade.kt` (등급 정의 엔티티)
- `src/main/kotlin/kr/ai/flori/customers/repository/CustomerGradeRepository.kt`
- `src/main/kotlin/kr/ai/flori/customers/service/CustomerGradeService.kt`
- `src/main/kotlin/kr/ai/flori/customers/controller/CustomerGradeController.kt`
- `src/main/kotlin/kr/ai/flori/customers/dto/CustomerGradeDtos.kt`

**수정**
- `src/main/kotlin/kr/ai/flori/customers/entity/Customer.kt` — `gradeId`, `gradeLocked` 추가, `grade` 제거
- `src/main/kotlin/kr/ai/flori/customers/service/CustomerService.kt` — 등급 해석/재계산, updateGrade(gradeId), revertAuto, 기본등급 시드
- `src/main/kotlin/kr/ai/flori/customers/controller/CustomerController.kt` — grade 엔드포인트 변경
- `src/main/kotlin/kr/ai/flori/customers/dto/CustomerDtos.kt` — 응답에 gradeId/gradeName/gradeLocked/photoThumbnails/photoCount
- `src/main/kotlin/kr/ai/flori/sales/service/SaleService.kt` — create/update/delete 에 등급 재계산 훅
- `src/main/kotlin/kr/ai/flori/photos/entity/PhotoCard.kt` — `customerId` 추가
- `src/main/kotlin/kr/ai/flori/photos/dto/PhotoCardDtos.kt` — customerId/customerName
- `src/main/kotlin/kr/ai/flori/photos/service/PhotoCardService.kt` — verifyCustomerOwnership, 직접 필터
- `src/main/kotlin/kr/ai/flori/photos/repository/PhotoCardRepository.kt` — findPage customer_id 직접 필터, clearCustomerReference
- `docs/sql/all-tables-ddl.sql` — SSOT 갱신

---

## Task 1: 마이그레이션 — photo_cards.customer_id

**Files:**
- Create: `docs/sql/migration/26-06-08-photo-cards-add-customer-id.sql`
- Modify: `docs/sql/all-tables-ddl.sql` (photo_cards 섹션)

- [ ] **Step 1: 마이그레이션 SQL 작성**

```sql
-- photo_cards 에 customer_id soft 참조 추가(FK 없음). 기존 sale 경유 연결분 백필.
BEGIN;

ALTER TABLE photo_cards ADD COLUMN IF NOT EXISTS customer_id BIGINT;

CREATE INDEX IF NOT EXISTS idx_photo_cards_user_customer
  ON photo_cards(user_id, customer_id);

-- 백필: sale_id가 있고 그 sale에 customer_id가 있는 카드는 그 고객으로 연결
UPDATE photo_cards pc
SET customer_id = s.customer_id
FROM sales s
WHERE pc.sale_id = s.id
  AND pc.customer_id IS NULL
  AND s.customer_id IS NOT NULL;

COMMIT;
```

- [ ] **Step 2: all-tables-ddl.sql 의 photo_cards CREATE TABLE 에 `customer_id BIGINT` + 인덱스 반영**

- [ ] **Step 3: 커밋**

```bash
git add docs/sql/migration/26-06-08-photo-cards-add-customer-id.sql docs/sql/all-tables-ddl.sql
git commit -m "feat(api): photo_cards customer_id 마이그레이션 + DDL"
```

---

## Task 2: 마이그레이션 — customer_grades + customers 컬럼

**Files:**
- Create: `docs/sql/migration/26-06-08-customer-custom-grades.sql`
- Modify: `docs/sql/all-tables-ddl.sql`

- [ ] **Step 1: 마이그레이션 SQL 작성**

```sql
-- 커스텀 등급 테이블 + customers.grade_id/grade_locked. 기존 grade(VARCHAR) 제거.
-- 기본 임계값 신규0/단골5/VIP10, 블랙리스트는 수동전용(threshold NULL).
BEGIN;

-- 1. 등급 정의 테이블
CREATE TABLE customer_grades (
  id BIGINT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  user_id BIGINT NOT NULL,
  name VARCHAR(50) NOT NULL,
  threshold INTEGER,                 -- NULL = 수동 전용(자동승급 대상 아님)
  sort_order INTEGER NOT NULL DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE (user_id, name)
);
CREATE INDEX idx_customer_grades_user ON customer_grades(user_id);

-- 2. 기존 테넌트별 기본 등급 시드
INSERT INTO customer_grades (user_id, name, threshold, sort_order)
SELECT u.id, g.name, g.threshold, g.sort_order
FROM users u
CROSS JOIN (VALUES
  ('신규', 0, 1),
  ('단골', 5, 2),
  ('VIP', 10, 3),
  ('블랙리스트', NULL::int, 4)
) AS g(name, threshold, sort_order)
ON CONFLICT (user_id, name) DO NOTHING;

-- 3. customers 컬럼 추가
ALTER TABLE customers ADD COLUMN IF NOT EXISTS grade_id BIGINT;
ALTER TABLE customers ADD COLUMN IF NOT EXISTS grade_locked BOOLEAN NOT NULL DEFAULT false;

-- 4. grade_id 백필 (기존 grade 문자열 → 시드 등급 매핑)
UPDATE customers c SET grade_id = cg.id
FROM customer_grades cg
WHERE cg.user_id = c.user_id
  AND cg.name = CASE c.grade
    WHEN 'new' THEN '신규'
    WHEN 'regular' THEN '단골'
    WHEN 'vip' THEN 'VIP'
    WHEN 'blacklist' THEN '블랙리스트'
    ELSE '신규'
  END;

-- 5. grade_locked: 블랙리스트였던 고객만 잠금(나머지는 자동 재계산)
UPDATE customers SET grade_locked = true WHERE grade = 'blacklist';

-- 6. 구 grade 컬럼 제거
ALTER TABLE customers DROP COLUMN grade;

COMMIT;
```

- [ ] **Step 2: all-tables-ddl.sql 갱신** — customer_grades 테이블 추가, customers 에서 `grade` 제거 + `grade_id BIGINT`, `grade_locked BOOLEAN NOT NULL DEFAULT false` 추가

- [ ] **Step 3: 커밋**

```bash
git add docs/sql/migration/26-06-08-customer-custom-grades.sql docs/sql/all-tables-ddl.sql
git commit -m "feat(api): customer_grades 테이블 + customers grade_id/grade_locked 마이그레이션"
```

---

## Task 3: CustomerGrade 엔티티 + 리포지토리

**Files:**
- Create: `src/main/kotlin/kr/ai/flori/customers/entity/CustomerGrade.kt`
- Create: `src/main/kotlin/kr/ai/flori/customers/repository/CustomerGradeRepository.kt`

- [ ] **Step 1: 엔티티 작성** (Customer.kt 패턴 답습)

```kotlin
package kr.ai.flori.customers.entity

import jakarta.persistence.*
import kr.ai.flori.common.entity.BaseEntity

/** 테넌트별 고객 등급 정의. threshold NULL = 수동 전용. (user_id, name) unique. */
@Entity
@Table(name = "customer_grades")
class CustomerGrade(
    @Column(name = "user_id", nullable = false)
    var userId: Long,
    @Column(name = "name", nullable = false)
    var name: String,
) : BaseEntity() {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    @Column(name = "id")
    var id: Long? = null

    @Column(name = "threshold")
    var threshold: Int? = null

    @Column(name = "sort_order", nullable = false)
    var sortOrder: Int = 0
}
```

> 참고: BaseEntity 가 `created_at`만 갖고 `updated_at`이 없는 경우 customer_grades DDL과 일치해야 함. 엔티티 작성 후 `./gradlew compileKotlin` 통과 확인.

- [ ] **Step 2: 리포지토리 작성**

```kotlin
package kr.ai.flori.customers.repository

import kr.ai.flori.customers.entity.CustomerGrade
import org.springframework.data.jpa.repository.JpaRepository

interface CustomerGradeRepository : JpaRepository<CustomerGrade, Long> {
    fun findByUserIdOrderBySortOrderAsc(userId: Long): List<CustomerGrade>
    fun findByIdAndUserId(id: Long, userId: Long): CustomerGrade?
    fun countByUserId(userId: Long): Long
    fun existsByUserIdAndNameAndIdNot(userId: Long, name: String, id: Long): Boolean
    fun existsByUserIdAndName(userId: Long, name: String): Boolean
}
```

- [ ] **Step 3: 컴파일 확인** — Run: `./gradlew compileKotlin` · Expected: BUILD SUCCESSFUL

- [ ] **Step 4: 커밋**

```bash
git add src/main/kotlin/kr/ai/flori/customers/entity/CustomerGrade.kt src/main/kotlin/kr/ai/flori/customers/repository/CustomerGradeRepository.kt
git commit -m "feat(api): CustomerGrade 엔티티 + 리포지토리"
```

---

## Task 4: 등급 CRUD (DTO·서비스·컨트롤러)

**Files:**
- Create: `src/main/kotlin/kr/ai/flori/customers/dto/CustomerGradeDtos.kt`
- Create: `src/main/kotlin/kr/ai/flori/customers/service/CustomerGradeService.kt`
- Create: `src/main/kotlin/kr/ai/flori/customers/controller/CustomerGradeController.kt`
- Test: `src/test/kotlin/kr/ai/flori/customers/service/CustomerGradeServiceTest.kt`

- [ ] **Step 1: DTO 작성**

```kotlin
package kr.ai.flori.customers.dto

import jakarta.validation.constraints.NotBlank
import jakarta.validation.constraints.PositiveOrZero
import kr.ai.flori.customers.entity.CustomerGrade

data class CustomerGradeCreateRequest(
    @field:NotBlank val name: String,
    @field:PositiveOrZero val threshold: Int? = null,  // null = 수동 전용
)

data class CustomerGradeUpdateRequest(
    val name: String? = null,
    val threshold: Int? = null,
    val sortOrder: Int? = null,
    val clearThreshold: Boolean = false,  // threshold 를 명시적으로 NULL(수동전용)로
)

data class CustomerGradeResponse(
    val id: Long,
    val name: String,
    val threshold: Int?,
    val sortOrder: Int,
) {
    companion object {
        fun from(g: CustomerGrade) = CustomerGradeResponse(
            id = requireNotNull(g.id), name = g.name, threshold = g.threshold, sortOrder = g.sortOrder,
        )
    }
}
```

- [ ] **Step 2: 실패 테스트 작성** (`CustomerGradeServiceTest`) — 생성/이름중복/삭제가드(마지막 1개 삭제 불가)

```kotlin
// 핵심 케이스(통합 테스트 스타일, 기존 *ServiceIntegrationTest 패턴 참고):
// - create: name+threshold 저장, sortOrder 자동 말미 배치
// - create 중복 이름 → AppException(CONFLICT)
// - delete: 마지막 남은 등급 삭제 시 AppException(VALIDATION)
// - delete: 그 등급 쓰던 고객 grade_id 처리(자동 재계산 또는 최저 등급)
```

Run: `./gradlew test --tests '*CustomerGradeServiceTest*'` · Expected: FAIL (서비스 미구현)

- [ ] **Step 3: 서비스 구현**

```kotlin
package kr.ai.flori.customers.service

import kr.ai.flori.common.error.AppException
import kr.ai.flori.common.error.CommonErrorCode
import kr.ai.flori.common.tenant.TenantContext
import kr.ai.flori.customers.dto.*
import kr.ai.flori.customers.entity.CustomerGrade
import kr.ai.flori.customers.repository.CustomerGradeRepository
import org.springframework.stereotype.Service
import org.springframework.transaction.annotation.Transactional

@Service
class CustomerGradeService(
    private val gradeRepository: CustomerGradeRepository,
) {
    @Transactional(readOnly = true)
    fun list(): List<CustomerGradeResponse> =
        gradeRepository.findByUserIdOrderBySortOrderAsc(TenantContext.currentUserId())
            .map(CustomerGradeResponse::from)

    @Transactional
    fun create(req: CustomerGradeCreateRequest): CustomerGradeResponse {
        val userId = TenantContext.currentUserId()
        if (gradeRepository.existsByUserIdAndName(userId, req.name))
            throw AppException(CommonErrorCode.CONFLICT, "이미 있는 등급명입니다")
        val nextOrder = (gradeRepository.findByUserIdOrderBySortOrderAsc(userId).maxOfOrNull { it.sortOrder } ?: 0) + 1
        val grade = CustomerGrade(userId, req.name).apply {
            threshold = req.threshold
            sortOrder = nextOrder
        }
        return CustomerGradeResponse.from(gradeRepository.save(grade))
    }

    @Transactional
    fun update(id: Long, req: CustomerGradeUpdateRequest): CustomerGradeResponse {
        val userId = TenantContext.currentUserId()
        val grade = gradeRepository.findByIdAndUserId(id, userId)
            ?: throw AppException(CommonErrorCode.NOT_FOUND, "등급을 찾을 수 없습니다")
        req.name?.let {
            if (gradeRepository.existsByUserIdAndNameAndIdNot(userId, it, id))
                throw AppException(CommonErrorCode.CONFLICT, "이미 있는 등급명입니다")
            grade.name = it
        }
        if (req.clearThreshold) grade.threshold = null
        else req.threshold?.let { grade.threshold = it }
        req.sortOrder?.let { grade.sortOrder = it }
        return CustomerGradeResponse.from(gradeRepository.save(grade))
    }

    @Transactional
    fun delete(id: Long) {
        val userId = TenantContext.currentUserId()
        val grade = gradeRepository.findByIdAndUserId(id, userId)
            ?: throw AppException(CommonErrorCode.NOT_FOUND, "등급을 찾을 수 없습니다")
        if (gradeRepository.countByUserId(userId) <= 1)
            throw AppException(CommonErrorCode.VALIDATION, "최소 1개 등급은 있어야 합니다")
        gradeRepository.delete(grade)
        // 이 등급을 쓰던 고객은 Task 5 의 재계산 경로로 정리(다음 매출/되돌리기 시) — 즉시 정리는 CustomerService.reassignAfterGradeDelete 로 호출
    }
}
```

> 삭제 시 그 등급을 참조하던 고객 즉시 정리는 Task 5 의 `CustomerService.recomputeForUser()` 또는 개별 `recomputeGrade()` 로 위임. 순환참조 피하려면 delete 에서 ApplicationEventPublisher 발행 후 CustomerService 리스너에서 처리하거나, CustomerGradeService 가 CustomerService 를 주입(단방향) — 후자 채택.

- [ ] **Step 4: 컨트롤러 작성**

```kotlin
package kr.ai.flori.customers.controller

import jakarta.validation.Valid
import kr.ai.flori.customers.dto.*
import kr.ai.flori.customers.service.CustomerGradeService
import org.springframework.web.bind.annotation.*

@RestController
@RequestMapping("/customer-grades")
class CustomerGradeController(
    private val service: CustomerGradeService,
) {
    @GetMapping fun list() = service.list()

    @PostMapping fun create(@Valid @RequestBody req: CustomerGradeCreateRequest) = service.create(req)

    @PatchMapping("/{id}")
    fun update(@PathVariable id: Long, @RequestBody req: CustomerGradeUpdateRequest) = service.update(id, req)

    @DeleteMapping("/{id}")
    fun delete(@PathVariable id: Long) = service.delete(id)
}
```

- [ ] **Step 5: 테스트 통과 확인** — Run: `./gradlew test --tests '*CustomerGradeServiceTest*'` · Expected: PASS

- [ ] **Step 6: 커밋**

```bash
git add src/main/kotlin/kr/ai/flori/customers/dto/CustomerGradeDtos.kt src/main/kotlin/kr/ai/flori/customers/service/CustomerGradeService.kt src/main/kotlin/kr/ai/flori/customers/controller/CustomerGradeController.kt src/test/kotlin/kr/ai/flori/customers/service/CustomerGradeServiceTest.kt
git commit -m "feat(api): 고객 등급 CRUD (GET/POST/PATCH/DELETE /customer-grades)"
```

---

## Task 5: Customer 엔티티 변경 + 등급 해석/재계산 로직

**Files:**
- Modify: `src/main/kotlin/kr/ai/flori/customers/entity/Customer.kt`
- Modify: `src/main/kotlin/kr/ai/flori/customers/service/CustomerService.kt`
- Test: `src/test/kotlin/kr/ai/flori/customers/service/CustomerGradeRecomputeTest.kt`

- [ ] **Step 1: Customer 엔티티 — `grade` 제거, `gradeId`/`gradeLocked` 추가**

```kotlin
// Customer.kt 본문에서 `var grade: String = "new"` 제거 후 아래 추가
@Column(name = "grade_id")
var gradeId: Long? = null

@Column(name = "grade_locked", nullable = false)
var gradeLocked: Boolean = false
```

- [ ] **Step 2: 재계산 로직 실패 테스트 작성** (`CustomerGradeRecomputeTest`)

```kotlin
// 케이스:
// - 구매 4회 → 단골(5) 미달 → 신규(0). 5회 → 단골. 10회 → VIP. (임계값 경계 4/5/10)
// - gradeLocked=true 면 구매 100회여도 등급 불변
// - revertGradeToAuto → gradeLocked=false + 즉시 재계산으로 적정 등급
// - 수동 updateGrade(gradeId) → gradeLocked=true
```

Run: `./gradlew test --tests '*CustomerGradeRecomputeTest*'` · Expected: FAIL

- [ ] **Step 3: CustomerService 에 등급 해석/재계산 메서드 추가 + updateGrade/revert 교체**

`CustomerService` 생성자에 `private val gradeRepository: CustomerGradeRepository` 주입 추가. 기존 `validGrade`/`GRADES`/`DEFAULT_GRADE` 제거. 아래 메서드 추가/교체:

```kotlin
/** 구매횟수 기준 적정 등급 id. threshold 있는 등급 중 threshold<=count 최대, 없으면 최저 sort_order. */
private fun autoGradeId(userId: Long, count: Int): Long? {
    val grades = gradeRepository.findByUserIdOrderBySortOrderAsc(userId)
    if (grades.isEmpty()) return null
    val eligible = grades.filter { it.threshold != null && it.threshold!! <= count }
        .maxByOrNull { it.threshold!! }
    return (eligible ?: grades.minByOrNull { it.sortOrder })?.id
}

/** 자동 등급 재계산(잠금 아니면). 매출 변경/되돌리기 후 호출. */
@Transactional
fun recomputeGrade(customerId: Long) {
    val userId = TenantContext.currentUserId()
    val customer = customerRepository.findByIdAndUserId(customerId, userId) ?: return
    if (customer.gradeLocked) return
    val count = statsFor(userId, customerId).purchaseCount   // CustomerStats 필드명에 맞춤
    val newGradeId = autoGradeId(userId, count)
    if (newGradeId != null && newGradeId != customer.gradeId) {
        customer.gradeId = newGradeId
        customerRepository.save(customer)
    }
}

/** 수동 등급 지정 → 잠금. */
@Transactional
fun updateGrade(id: Long, gradeId: Long): CustomerResponse {
    val customer = load(id)
    gradeRepository.findByIdAndUserId(gradeId, customer.userId)
        ?: throw AppException(CommonErrorCode.VALIDATION, "올바르지 않은 등급입니다")
    customer.gradeId = gradeId
    customer.gradeLocked = true
    return CustomerResponse.from(customerRepository.save(customer), statsFor(customer.userId, id), gradeNameOf(customer))
}

/** 자동 등급으로 되돌리기 → 잠금 해제 후 재계산. */
@Transactional
fun revertGradeToAuto(id: Long): CustomerResponse {
    val customer = load(id)
    customer.gradeLocked = false
    customer.gradeId = autoGradeId(customer.userId, statsFor(customer.userId, id).purchaseCount)
    return CustomerResponse.from(customerRepository.save(customer), statsFor(customer.userId, id), gradeNameOf(customer))
}
```

> `statsFor` 의 count 필드명은 실제 `CustomerStats` 정의에 맞춰 사용(`purchaseCount` 가정 — 다르면 맞춤). `CustomerResponse.from` 시그니처는 Task 6 에서 gradeName 인자 받도록 확장.

- [ ] **Step 4: 기본 등급 시드 보장(신규 테넌트)** — 등급 조회/사용 진입점에서 비어 있으면 기본 4종 생성하는 `ensureDefaultGrades(userId)` 를 `CustomerGradeService.list()`·고객 생성 경로에 추가

```kotlin
// CustomerGradeService 에 추가
fun ensureDefaults(userId: Long) {
    if (gradeRepository.countByUserId(userId) > 0L) return
    listOf("신규" to 0, "단골" to 5, "VIP" to 10).forEachIndexed { i, (n, t) ->
        gradeRepository.save(CustomerGrade(userId, n).apply { threshold = t; sortOrder = i + 1 })
    }
    gradeRepository.save(CustomerGrade(userId, "블랙리스트").apply { threshold = null; sortOrder = 4 })
}
// list() 첫 줄에서 ensureDefaults(TenantContext.currentUserId()) 호출
```

- [ ] **Step 5: 테스트 통과** — Run: `./gradlew test --tests '*CustomerGradeRecomputeTest*'` · Expected: PASS

- [ ] **Step 6: 커밋**

```bash
git add src/main/kotlin/kr/ai/flori/customers/
git commit -m "feat(api): 고객 등급 자동승급/수동고정/되돌리기 + 기본등급 시드"
```

---

## Task 6: CustomerResponse 확장 (gradeName/gradeLocked + 사진 썸네일/카운트)

**Files:**
- Modify: `src/main/kotlin/kr/ai/flori/customers/dto/CustomerDtos.kt`
- Modify: `src/main/kotlin/kr/ai/flori/customers/service/CustomerService.kt`
- Modify: `src/main/kotlin/kr/ai/flori/customers/controller/CustomerController.kt`

- [ ] **Step 1: CustomerResponse 필드 추가** — `gradeId: Long?`, `grade: String?`(등급명, 배지용), `gradeLocked: Boolean`, `photoThumbnails: List<String>`, `photoCount: Int`. `from(...)` 시그니처에 gradeName·thumbnails·count 인자 추가(목록은 일괄 맵, 단건은 개별 조회)

- [ ] **Step 2: 목록 일괄 집계 추가** — `CustomerService.list()` 에서 등급명 맵(`gradeRepository.findByUserIdOrderBySortOrderAsc` → id→name)과 사진 집계를 한 번에 조회

```kotlin
// 고객별 대표 썸네일 3장 + 카운트 (N+1 없이 1쿼리). photos jsonb 의 첫 url 추출.
private fun photoSummaryByCustomer(userId: Long): Map<Long, Pair<List<String>, Int>> =
    jdbcTemplate.query(
        """
        SELECT customer_id,
               count(*) AS cnt,
               (array_agg((photos->0->>'url') ORDER BY created_at DESC)
                  FILTER (WHERE jsonb_array_length(photos) > 0))[1:3] AS thumbs
        FROM photo_cards
        WHERE user_id = ?::bigint AND customer_id IS NOT NULL
        GROUP BY customer_id
        """.trimIndent(),
        { rs, _ ->
            val arr = rs.getArray("thumbs")?.array as? Array<*>
            val thumbs = arr?.filterIsInstance<String>() ?: emptyList()
            rs.getLong("customer_id") to (thumbs to rs.getInt("cnt"))
        },
        userId,
    ).toMap()
```

> `photos->0->>'url'` 는 카드 첫 사진 url. 카드별 대표 1장씩 모아 최신 3장. 실제 jsonb 구조(`[{url,originalName}]`)에 맞춤. 동작은 통합테스트로 검증.

- [ ] **Step 3: 단건 `get()`** — 해당 고객 photo 썸네일/카운트 개별 조회 메서드 추가, gradeName 포함해 응답

- [ ] **Step 4: 컨트롤러 grade 엔드포인트 교체**
  - `PATCH /customers/{id}/grade` body `{ gradeId: Long }` → `service.updateGrade(id, gradeId)`
  - `PATCH /customers/{id}/grade/auto` → `service.revertGradeToAuto(id)`

- [ ] **Step 5: 컴파일 + 기존 고객 테스트 회귀** — Run: `./gradlew test --tests '*Customer*'` · Expected: PASS (기존 grade 문자열 테스트는 gradeId 기반으로 수정)

- [ ] **Step 6: 커밋**

```bash
git add src/main/kotlin/kr/ai/flori/customers/
git commit -m "feat(api): 고객 응답에 등급명/잠금 + 대표 사진 썸네일/카운트"
```

---

## Task 7: SaleService 등급 재계산 훅

**Files:**
- Modify: `src/main/kotlin/kr/ai/flori/sales/service/SaleService.kt`
- Test: `src/test/kotlin/kr/ai/flori/sales/service/SaleGradePromotionTest.kt`

- [ ] **Step 1: 실패 테스트** — 매출 5건 등록 후 고객 등급이 '단골'로 자동 승급 / 잠긴 고객은 불변 / 매출 삭제로 4건 되면 '신규'로 강등(자동인 경우)

Run: `./gradlew test --tests '*SaleGradePromotionTest*'` · Expected: FAIL

- [ ] **Step 2: 훅 삽입** — `SaleService` 에 `CustomerService` 주입(이미 있으면 재사용). `create()`/`update()`/`delete()` 에서 매출이 특정 customerId 와 연결/해제된 직후 `customerService.recomputeGrade(customerId)` 호출. customerId 가 바뀐 update 는 이전·이후 고객 둘 다 재계산.

```kotlin
// create(): resolveCustomerId 결과가 non-null 이고 save 후
resolvedCustomerId?.let { customerService.recomputeGrade(it) }
// delete(): 삭제 전 sale.customerId 보관 후 삭제 → 보관값 non-null 이면 recompute
// update(): oldCustomerId, newCustomerId 각각 non-null 이면 recompute (중복 제거)
```

- [ ] **Step 3: 테스트 통과** — Run: `./gradlew test --tests '*SaleGradePromotionTest*'` · Expected: PASS

- [ ] **Step 4: 커밋**

```bash
git add src/main/kotlin/kr/ai/flori/sales/service/SaleService.kt src/test/kotlin/kr/ai/flori/sales/service/SaleGradePromotionTest.kt
git commit -m "feat(api): 매출 변경 시 고객 등급 자동 재계산 훅"
```

---

## Task 8: PhotoCard customer_id (엔티티·DTO·서비스·필터)

**Files:**
- Modify: `src/main/kotlin/kr/ai/flori/photos/entity/PhotoCard.kt`
- Modify: `src/main/kotlin/kr/ai/flori/photos/dto/PhotoCardDtos.kt`
- Modify: `src/main/kotlin/kr/ai/flori/photos/service/PhotoCardService.kt`
- Modify: `src/main/kotlin/kr/ai/flori/photos/repository/PhotoCardRepository.kt`
- Modify: `src/main/kotlin/kr/ai/flori/customers/service/CustomerService.kt` (delete 시 photo clear)
- Test: `src/test/kotlin/kr/ai/flori/photos/service/PhotoCardCustomerTest.kt`

- [ ] **Step 1: 엔티티에 컬럼 추가** — PhotoCard.kt 에 `@Column(name = "customer_id") var customerId: Long? = null` (saleId 옆)

- [ ] **Step 2: DTO** — `PhotoCardCreateRequest`/`UpdateRequest` 에 `customerId: Long?`, `PhotoCardResponse` 에 `customerId: Long?` + `customerName: String?`(연결 고객명, 배지용)

- [ ] **Step 3: 실패 테스트** — create 시 customerId 소유 검증(타테넌트 고객 → 예외), `GET /photo-cards?customerId=` 가 customer_id 직접 매칭, 응답에 customerName 노출

Run: `./gradlew test --tests '*PhotoCardCustomerTest*'` · Expected: FAIL

- [ ] **Step 4: 서비스** — `verifyCustomerOwnership(customerId)` 추가(`verifySaleOwnership` 패턴: CustomerRepository.findByIdAndUserId null 이면 예외), create/update 에서 customerId non-null 시 검증·세팅. 응답 매핑 시 customerName 채움(연결 고객 일괄/개별 조회)

- [ ] **Step 5: 리포지토리 `findPage` 필터 교체** — sales LEFT JOIN 제거, `(:customerId IS NULL OR pc.customer_id = :customerId)` 직접 필터. `clearCustomerReference(userId, customerId)` 추가(고객 삭제 시 nullify)

- [ ] **Step 6: 고객 삭제 정리** — `CustomerService.delete()` 에 `photoCardRepository.clearCustomerReference(userId, id)` 추가(sales clear 옆). 순환주입 피하려면 PhotoCardRepository 만 주입

- [ ] **Step 7: 테스트 통과** — Run: `./gradlew test --tests '*PhotoCard*'` · Expected: PASS

- [ ] **Step 8: 커밋**

```bash
git add src/main/kotlin/kr/ai/flori/photos/ src/main/kotlin/kr/ai/flori/customers/service/CustomerService.kt src/test/kotlin/kr/ai/flori/photos/service/PhotoCardCustomerTest.kt
git commit -m "feat(api): photo_cards customer_id 직접 연결 + 소유검증 + 직접 필터"
```

---

## Task 9: REST Docs + 전체 회귀 + 빌드

**Files:**
- Modify: 관련 `*DocsTest.kt` (customer-grades, photo-card customerId, customer grade 응답)

- [ ] **Step 1: REST Docs 테스트 갱신/추가** — customer-grades CRUD 문서, photo-card customerId 필드, customer 응답 신규 필드(gradeId/grade/gradeLocked/photoThumbnails/photoCount)

- [ ] **Step 2: 전체 테스트** — Run: `./gradlew test` · Expected: BUILD SUCCESSFUL

- [ ] **Step 3: 전체 빌드** — Run: `./gradlew build` · Expected: BUILD SUCCESSFUL

- [ ] **Step 4: 커밋**

```bash
git add src/test/
git commit -m "test(api): 세션3 REST Docs + 회귀"
```

---

## 자체 검토 메모

- **spec 커버리지**: 기능1(사진customer_id) Task1/8 · 기능2(등급) Task2~7 · 응답확장 Task6 ✅
- **타입 일관성**: `recomputeGrade`/`updateGrade`/`revertGradeToAuto`/`autoGradeId`/`ensureDefaults`/`verifyCustomerOwnership`/`clearCustomerReference` 명칭 통일
- **DB 적용**: 마이그레이션 2종은 RDS 적용 **사용자 허락 후**. `ddl-auto: validate` 라 테스트 전 스키마 일치 필요(testcontainers init SQL 또는 적용 DB)
- **확인 필요(구현 중)**: `CustomerStats` count 필드명, `CustomerResponse.from` 시그니처, BaseEntity 의 updated_at 유무, photos jsonb array_agg 문법 — 실제 코드에 맞춰 조정
